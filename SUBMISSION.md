# moonchip · 黑客松申报书

## 一、项目简介

**moonchip —— 完整的 CHIP-8 虚拟机，取指/译码/执行循环全部由 MoonBit 编译为 WebAssembly。**
35 个操作码、64×32 显示、延迟/声音定时器、键盘、怪癖开关一应俱全；浏览器里插入卡带即玩（Brix、Space Invaders、Blinky、Pong、Tank），并用 Timendus 官方测试套件做可视化外部验证。

## 二、背景与动机

- 「用 MoonBit 写模拟器」是对这门语言最有说服力的检验：一个解释器热循环 + 精确的字节级语义 + 实时约束，比任何滤镜基准都更能暴露工具链的真实成色。
- CHIP-8 是模拟器文化的入口：ISA 只有 35 条指令，却有海量公版 ROM 和一个**公认的第三方验证标准**（Timendus 测试套件）——做对了当场可见，做错了也当场可见，没法自欺。
- 我们想交付的不是一个玩具，而是一台能跑真游戏、经得起外部套件审计、每个怪癖都有开关和测试的机器。

## 三、功能亮点

- **完整 CHIP-8**：4 KB RAM、16 寄存器、16 级栈、60 Hz 定时器、`0x50` 字库、`0x200` 装载； halted（栈溢出/未知指令/`00FD` 退出）干净停止并在 UI 显示状态。
- **12 个卡带**：Timendus 测试套件 7 个（logo 动画、IBM logo、Corax+、flags、quirks、keypad、beep）+ 经典游戏 5 个（Brix / Space Invaders / Blinky / Pong / Tank，公版可再分发）。
- **四个怪癖开关**（VF 复位、FX55/FX65 递增 I、移位用 VY、精灵边缘裁剪）实时切换，配测试套件当场看行为差异——这是 CHIP-8 社区三十年怪癖之争的交互式教材。
- **复古机箱 UI**：琥珀色磷光屏、16 键虚拟键盘（屏上可点 + 1234/qwer/asdf/zxcv 物理键盘）、实时 CPU 状态面板（PC/I/SP/定时器/V0–VF/指令计数）、WebAudio 方波蜂鸣器。
- **可玩性**：IBM logo 开机即动，Brix 按键开局、4/6 键控板、真实计分。

## 四、技术架构与实现（MoonBit 技术要点）

1. **`src/lib`** — 纯 MoonBit VM 核心：单字节取指、大 switch 译码、XOR 精灵绘制（碰撞写入 VF）、定时器、键 awaited `FX0A`、xorshift PRNG（宿主播种，全程确定性）。16 个单元测试全部用手写机器码程序驱动。
2. **`src/kernel`** — wasm `foreign_library`：每帧一次 `tick(N)` + 一次显存快照读取（64×32 位图直出线性内存 → `ImageData` → 放大 blit）；ROM 以原始字节写入暂存区装载；键盘/怪癖/调试状态是普通导出调用。实测吞吐**每秒数百万帧**，60 Hz 是刻意节流。
3. **`web/`** — 机箱 UI + 测试套件 + 零依赖服务器 + 公版 ROM 库。

**关键技术攻关**（测试套件抓出来的真 bug，单测全绿也没拦住）：

- **`5XY0`/`9XY0` 的判定子是最低半字节**，不是 Y——第一版写在 Y 上，Timendus 套件跑到 `5420` 直接halt。修正后单测补上双路断言。
- **`00FD`（退出指令）必须按完整操作码匹配**——第一版写成 `nnn == 0x0FFF` 永不匹配，套件跑完判定页后踩进垃圾指令崩溃。同样的教训从 json-repair 的围栏处理一路延续过来：**边界上的字节语义必须用真机数据钉死**。
- **确定性设计**：CXNN 的随机数来自内部 xorshift、宿主播种——同 ROM + 同输入 + 同种子逐字节复现（内核测试断言两次运行的亮像素数完全一致），这为将来的回放/联网对战留下地基。
- MoonBit 工程实录：`0xC0.to_byte()` 会被解析成浮点字面量需加括号；大写标识符不能作顶层 `let`；`const` 不接受 FixedArray 类型；`&` 与 `==` 的优先级和 C 不同。

## 五、性能与验证

- **吞吐**：Node 单线程实测 Brix 每秒模拟 **374 万**个 60 Hz 帧——实时需求（每秒 60 帧）的六万倍，浏览器 60fps 渲染毫无压力。
- **质量门槛全绿**：`moon check` 零警告；`moon test` **16/16**；内核端到端 **14/14**。
- **真机验证**（内核测试，真实 ROM）：IBM logo 点亮 230 像素、chip8-logo 连续动画 120 帧、7-beep 驱动声音定时器、Brix 标题屏渲染、种子化随机绘制两次运行逐字节一致。
- **外部审计**：Timendus 套件在浏览器里可视化运行——平台选择、判定页（VF RESET ON / MEMORY OFF / DISP.WAIT OFF / CLIPPING ON，与实现配置一致）、keypad 交互测试全部工作。开发过程中它抓出了两个单测漏掉的真解码 bug（见上）。
- **真实浏览器端到端**：Brix 实机游玩（按键开局、控板、得分、掉命），IBM logo 逐列滚动动画，蜂鸣器随声音定时器发声。

## 六、运行与演示

```bash
moon build --target wasm --release
cp _build/wasm/release/build/src/kernel/kernel.wasm web/moonchip.wasm
node web/server.mjs 8097
# 打开 http://127.0.0.1:8097
```

30 秒演示脚本：IBM logo 开机动画自动播放 → 点 **5 · quirks**，按 1 选 CHIP-8 平台，看怪癖判定页 → 点 **Brix**，按 1 开局、4/6 键打砖块 → 打开「CPU 状态」面板边玩边看寄存器跳动 → 切换怪癖开关重跑套件对比判定页。

## 七、展望

- **SUPER-CHIP / XO-CHIP**：扩展模式与更高分辨率（套件菜单里的 2/3 选项已经等着被点亮）；
- **存档点与回放**：确定性内核已就绪，录制输入流即可实现即时回放、ghost 对战与关卡分享；
- **调试器**：单步执行、断点、内存视图（state_out 导出已备好）；
- **cassette 生态**：接入 kripod 档案全集 + 在线 ROM 清单；
- **生态**：解释器循环抽成 mooncakes.io 通用组件（chip8-core），供教学与玩具 OS 使用。
