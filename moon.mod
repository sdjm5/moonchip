name = "SDJM5/moonchip"
version = "0.1.0"
readme = "README.md"
repository = "https://github.com/SDJM5/moonchip"
license = "Apache-2.0"
keywords = [
  "emulator",
  "chip8",
  "retro",
  "wasm",
  "web"
]
preferred_target = "wasm"
description = "moonchip: a CHIP-8 virtual machine in MoonBit compiled to WebAssembly — 35 opcodes, quirk switches, sound and display, verified against the Timendus test suite in the browser."
